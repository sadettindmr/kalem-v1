# ===========================================
# Kalem v1.0.0 - Kasghar - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ===========================================
# API KEYS (User must obtain these)
# ===========================================

# Semantic Scholar API Key
# Get yours at: https://www.semanticscholar.org/product/api
# Without this key, rate limits are very low (100 requests/5 min)
# With key: 100 requests/second
SEMANTIC_SCHOLAR_API_KEY=

# CORE API Key (Optional - for CORE academic search)
# Get yours at: https://core.ac.uk/services/api
CORE_API_KEY=

# OpenAI API Key (Optional - for LLM-powered answers)
# Get yours at: https://platform.openai.com/api-keys
# Only needed if you want to use the LLM feature in chat
OPENAI_API_KEY=

# ===========================================
# APPLICATION SETTINGS
# ===========================================

# Debug mode (set to false in production)
DEBUG=true

# Mock mode - use fake data instead of real API calls (for testing)
MOCK_MODE=false

# Embedding dimension (768 for all-mpnet-base-v2, 384 for MiniLM)
EMBEDDING_DIM=768

# Optional outbound proxy for all external HTTP requests
# Example: http://user:pass@proxy.example.com:8080
OUTBOUND_PROXY=

# ===========================================
# DATABASE (Docker defaults - change if needed)
# ===========================================

# PostgreSQL connection URL
DATABASE_URL=postgresql+asyncpg://athena:secure_password@postgres_db:5432/athena_core

# PostgreSQL credentials (used by docker-compose)
POSTGRES_USER=athena
POSTGRES_PASSWORD=secure_password
POSTGRES_DB=athena_core

# ===========================================
# CACHE & QUEUE (Docker defaults)
# ===========================================

# Redis URL
REDIS_URL=redis://redis_cache:6379/0

# RabbitMQ credentials (used by docker-compose)
RABBITMQ_DEFAULT_USER=athena
RABBITMQ_DEFAULT_PASS=rabbitmq_password

# RabbitMQ URL (Celery broker)
CELERY_BROKER_URL=amqp://athena:rabbitmq_password@rabbitmq_broker:5672//

# Celery result backend
CELERY_RESULT_BACKEND=redis://redis_cache:6379/0

# ===========================================
# NOTES
# ===========================================
# 
# 1. API Keys are YOUR responsibility to obtain and manage
# 2. Never commit your .env file to version control
# 3. Keep your API keys secure and don't share them
# 4. Check each service's terms of use before using
#
# For more information, see README.md
